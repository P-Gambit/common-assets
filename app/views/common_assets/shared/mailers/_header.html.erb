<wrapper class="header">
	<container>
		<row class="collapse">
			<columns small="6">
				<a href="<%= Setting.get(:CMS_DOMAIN) %>">
					<img alt="YugenTrade" src="<%= URI.join(Setting.get(:CMS_DOMAIN), image_url('ygn-logotype.png')) %>" width="120" style="width: 100%; max-width: 120px; font-family: sans-serif; color: #ffffff; font-size: 20px; display: block; border: 0px;" border="0"></a>
			</columns>
			<columns small="6" class="links">
				<button class="small radius warning btn" href="<%= Setting.get(:CMS_DOMAIN) %>/trading">Trade</button>
			</columns>
		</row>
	</container>
</wrapper>

<spacer size="16"></spacer>